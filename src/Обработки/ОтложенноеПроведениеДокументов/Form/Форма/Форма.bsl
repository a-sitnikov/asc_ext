
&НаКлиенте
Процедура ВыполнитьПроведение(Команда)
	ВыполнитьПроведениеНаСервере();
КонецПроцедуры

&НаСервере
Процедура ВыполнитьПроведениеНаСервере()
	
	ОбработкаОбъект = РеквизитФормыВЗначение("Объект");
	Результат = ОбработкаОбъект.ОтложенноеПроведениеДокументов();
	
	Если Не Результат Тогда
		Сообщить("Отложенное проведение уже запущено");
	Иначе	
		Сообщить("Отложенное проведение выполнено");
	КонецЕсли;	
	
КонецПроцедуры
